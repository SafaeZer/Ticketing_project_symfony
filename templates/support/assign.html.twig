{% extends 'support.html.twig' %}

{% block body %}
	{{ form_start(form) }}
	<div class="container mt-5">
		<div class="col-6 mx-auto">
			{{ form_widget(form) }}
			<button class="btn btn-success">{{ button_label|default('Assign') }}</button>
		</div>
	</div>
	{{ form_end(form) }}


{% endblock %}
